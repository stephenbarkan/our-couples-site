{% macro recipeLink(title, searchterms, image, url) %}

	<a href="{{ url }}" class="relative flex flex-col justify-between h-full py-6 space-y-3 font-light text-black border-b-2 border-black border-opacity-75">
                            <ul class="flex flex-wrap -mx-1 -my-1 text-lg font-normal text-black text-opacity-75">
                        {% for term in searchterms | sort %}
                            <li>
                                <span class="mx-1 my-1">{{ term }}</span>
                                {% if loop.index < loop.length %}
                                    <span class="mx-1 my-1 text-black text-opacity-50">•</span>
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ul>
            <div class="flex items-center space-x-4">
                <div class="flex-shrink-0 w-48 p-2 origin-center bg-white border-2 border-black border-opacity-75">
    			    <img src="{{ image }}" class="object-cover w-full h-full" alt=""/>
    		    </div>
                <h2 class="text-4xl leading-tight ">{{ title }}</h2>
            </div>
			{# <div class="flex-shrink-0 w-8 text-black">
				{% include "icons/right-arrow.svg" %}
			</div> #}
	</a>

{% endmacro %}
